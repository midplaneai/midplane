{
  "name": "midplane-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Safety layer between AI coding agents and Postgres. Workspace root.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "test": "bun test",
    "smoketest": "cd examples/smoketest && ./run.sh"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
