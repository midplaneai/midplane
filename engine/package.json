{
  "name": "midplane-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Query-path engine behind Midplane's Postgres MCP server for AI agents: mask, policy, approvals, audit. Engine subtree of midplane-cloud; members are declared by the root workspaces globs (engine/packages/*, engine/examples/*), not here.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:cov": "bun test --coverage",
    "smoketest": "cd examples/smoketest && ./run.sh"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
